#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int counter;

	while (counter < 10)
	{
		printf("%d", counter);
		counter++;
	}
	return (0);
}
